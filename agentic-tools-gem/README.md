# agentic-tools-gem

Gem tool package with a shared `register_tools(registry)` entrypoint and generated `tool_catalog.json`.
Write handlers still implement preview branches for internal runtime dry runs and verification, but this package does not define separate `_stage` tool IDs.
